
' # =============================================================================
' #### Создание и заполнение двумерного массива в VBA Access
' # =============================================================================
' #### **1. Объявление двумерного массива**
Dim myArray(1 To 3, 1 To 4) As Integer
' ```
' Здесь:
' - `1 To 3` — количество строк (3 строки).
' - `1 To 4` — количество столбцов (4 столбца).
' - `As Integer` — тип данных массива (в данном случае целые числа).

' # =============================================================================
' #### **2. Заполнение массива данными**

' Двумерный массив можно заполнить данными с помощью цикла `For`:

Sub FillArray()
    Dim myArray(1 To 3, 1 To 4) As Integer
    Dim i As Integer, j As Integer
    
    ' Заполнение массива
    For i = 1 To 3
        For j = 1 To 4
            myArray(i, j) = i * j ' Пример: произведение индексов
        Next j
    Next i
    
    ' Вывод данных массива в окно сообщений
    For i = 1 To 3
        For j = 1 To 4
            Debug.Print "myArray(" & i & ", " & j & ") = " & myArray(i, j)
        Next j
    Next i
End Sub

' # =============================================================================
' #### **3. Использование динамического массива**
' Если размер массива заранее неизвестен, можно использовать динамический массив. 
' Для этого применяется ключевое слово `ReDim`:

Sub DynamicArray()
    Dim myArray() As Integer
    Dim rows As Integer, cols As Integer
    Dim i As Integer, j As Integer
    
    ' Определение размеров массива
    rows = 3
    cols = 4
    ReDim myArray(1 To rows, 1 To cols)
    
    ' Заполнение массива
    For i = 1 To rows
        For j = 1 To cols
            myArray(i, j) = i + j ' Пример: сумма индексов
        Next j
    Next i
    
    ' Вывод данных массива
    For i = 1 To rows
        For j = 1 To cols
            Debug.Print "myArray(" & i & ", " & j & ") = " & myArray(i, j)
        Next j
    Next i
End Sub


'=========================================================================
'   ПЕРЕИМЕНОВАНИЕ ПОЛЕЙ В [РЕЙСЫ] ДЛЯ ОПРЕДЕЛЕНИЯ ПЛАНОВ
'=========================================================================
Function rRenamingFields_РЕЙСЫ()
    Dim i, cols
    ' Определение размеров массива
    ReDim tFields(1 To 8, 1 To 8)
    ' Унификацированные названия поолей
    tFields(1, 1) = "Контрагент"
    tFields(2, 1) = "Рейс-1_Контрагент"
    tFields(1, 2) = "Договор"
    tFields(2, 2) = "Рейс-1_Договор"
    tFields(1, 3) = "Месяц, гг"
    tFields(2, 3) = "Рейс-1_Месяц погрузки"
    tFields(1, 4) = "СтОтпр"
    tFields(2, 4) = "Рейс-1_ст отпр"
    tFields(1, 5) = "СтНазн"
    tFields(2, 5) = "Рейс-1_ст назн"
    tFields(1, 6) = "ДорНзн"
    tFields(2, 6) = "Рейс-1_Дорога назн"
    tFields(1, 7) = "ГрпГруза"
    tFields(2, 7) = "Рейс-1_Группа груза"
    tFields(1, 8) = "ДорОтпр"
    tFields(2, 8) = "Рейс-1_Дорога отпр"
    
    ' Вывод матрицы
    cols = 8
    For i = 1 To cols
        Debug.Print tFields(1, i) & vbTab & vbTab & tFields(2, i)
    Next i
End Function
